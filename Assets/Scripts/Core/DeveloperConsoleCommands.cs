using System;
using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using System.Net.NetworkInformation;
using Random = UnityEngine.Random;

public static class DeveloperConsoleCommands {
	
	//public static Behaviour[] scenarios = Resources.LoadAll<Behaviour>("Prefabs/Events");
	
	public static void Begin(string command) {
	    if (command == "") return;
		
		//command = command.ToLower();
        //Split the command string by spaces to check for different input information
        //string[] splits = command.Split(null);

	}
}
